If you use the semicolon between two commands, the second will be always executed even if the previous fail.

ls /etc/mannaggiasangennaro; ls /etc/hosts

The 1st will fail and the second will get always executed. 
